<h1>Create a team.</h1>
<%= link_to 'Or add yourself to a team', :controller => 'members', :action => 'new' %>
<%= form_for @teams do |f| %>
	<%= f.label :title %> :
	<%= f.text_field 'title' %>
	
	<%= f.submit 'create' %>
<% end %>